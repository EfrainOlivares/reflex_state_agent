require 'spec_helper'

describe ReflexStateAgent do
  it 'has a version number' do
    expect(ReflexStateAgent::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
